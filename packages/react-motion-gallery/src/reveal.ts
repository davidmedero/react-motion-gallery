"use client";

export { Reveal, Reveal as default, resolveRevealTransform, useReveal } from "./Gallery/reveal/Reveal";

export type {
  RevealAngle,
  RevealChannelOptions,
  RevealDuration,
  RevealEasing,
  RevealLength,
  RevealMotionChannel,
  RevealOptions,
  RevealProps,
  RevealTransform,
  RevealTransformObject,
  RevealVariant,
  UseRevealResult,
} from "./Gallery/reveal/Reveal";
